# PETS-U parity gate

`ETTH128_STANDARD_PETSU_PARITY_PASS`

The production loader and the isolated reviewer loader are exact through state dict, selected periods, candidate weights, candidate branch outputs, encoder/decoder fused features, denoiser output, and one reverse step. Every maximum absolute error is at most `1e-7`.
