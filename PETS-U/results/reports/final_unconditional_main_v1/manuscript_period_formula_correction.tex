% Audited discrete-period correction
The discrete candidate period is $p_j=\lfloor T_s/f_j\rfloor$. For the two-dimensional period branch, the temporal axis is padded separately to $\lceil T_s/p_j\rceil p_j$ before reshaping, and is cropped back to $T_s$ afterward.
