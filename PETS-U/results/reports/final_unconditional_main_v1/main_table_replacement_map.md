# Main-table replacement map

All unresolved legacy unconditional cells are deleted and replaced by the corresponding controlled row in `paper_table_unconditional_main.tex`. The exactly confirmed ETTh-128 anchor is retained through exact checkpoint reuse and common re-evaluation. No incompatible historical value remains.
